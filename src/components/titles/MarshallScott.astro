<div class="title">
    <h1><span>H</span><span>i</span><span>,</span></h1>
    <h1>
        <span>I</span><span>'</span><span>m</span><div class="space">&nbsp;</div><span>M</span><span>a</span><span>r</span><span>s</span><span>h</span><span>a</span><span>l</span><span>l</span>
    </h1>
</div>

<p class="subtitle">
    I'm a Software Engineer with a passion for computer graphics & security.
</p>

<style lang="scss">
  $ease: cubic-bezier(0.0, 0.0, 0.50, 1.0);
  
	.title {

    animation: title-fade 500ms;
    animation-timing-function: $ease;

		h1 {
			color: white;
			font-size: min(5rem, 12vw);
			display: flex;
			margin-bottom: 0px;
			line-height: 1.2;
		}

		.space {
			white-space: nowrap;
		}
	}

  .subtitle {
    animation: subtitle-fade 500ms forwards;
    animation-timing-function: $ease;
  }

  @keyframes title-fade {
    from {
      opacity: 0;
      transform: translateY(2rem);
    }
    to {
      opacity: 1;
      transform: translateY(0rem);
    }
  }

  @keyframes subtitle-fade {
    from {
      opacity: 0;
      transform: translateY(2.5rem);
      color: #ffffff;
    }
    to {
      opacity: 1;
      transform: translateY(0rem);
      color: #0b0b0b;
    }
  }

  .title span {
    animation: letter-fade 1s forwards;
    animation-timing-function: $ease;
  }

  @keyframes letter-fade {
    0% {
      color: hsla(130, 40%, 82%, 0);
    }
    55% {
      color: hsl(255, 40%, 82%);
    }
    56% {
      color: hsl(0, 40%, 82%);
    }
    100% {
      color: hsl(106, 24%, 52%);
    }
  }

  $letters: 17;

  @for $i from 1 through $letters {
    .title span:nth-child(#{$i}) {
      animation-delay: $i * 20ms;
    }
  }
</style>
